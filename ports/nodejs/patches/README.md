# Patches

Node.js 24.18.1 already accepts `--dest-os=ios`. Version-specific source
patches discovered during the first complete Xcode build belong in this
directory and must be applied explicitly from `scripts/build.sh`.
